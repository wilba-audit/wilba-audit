// Trigger.dev scaffold. Project is not initialized yet.
// To register: `npx trigger.dev@latest init` then fill in the project ref below.
export const triggerConfig = {
  project: "<REPLACE_WITH_PROJECT_REF>",
  dirs: ["./src/trigger"],
  runtime: "node",
} as const;
